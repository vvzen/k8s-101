apiVersion: v1
kind: Service

metadata:
  name: my-app-svc

spec:
  type: NodePort
  selector:
    app: my-app-deployment
  ports:
    - port: 80  # Port exposed within the cluster
      targetPort: 8000  # Port on the pods
